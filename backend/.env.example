PORT=3000
JWT_SECRET=replace-me-with-a-long-random-string
JWT_EXPIRES_IN=7d

DB_HOST=localhost
DB_PORT=3306
DB_USER=teamup
DB_PASSWORD=teamup
DB_NAME=teamup

# GIPHY API key (server-side proxy for GIF search). Get a free beta key at
# developers.giphy.com. Without it, /api/giphy/search returns 503.
GIPHY_API_KEY=replace-me
